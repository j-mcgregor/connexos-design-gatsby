import * as React from 'react'
import Layout from '../components/Layout'

const Product = props => {
    console.log(props)
    return (
        <Layout>
            <h1>Product</h1>
        </Layout>
    )
}

export default Product
